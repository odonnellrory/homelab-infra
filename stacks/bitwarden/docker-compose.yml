services:
  bitwarden:
    image: ghcr.io/bitwarden/lite
    restart: always
    env_file:
      - ${HOMELAB_DATA}/bitwarden/settings.env
    volumes:
      - bwdata:/etc/bitwarden
    ports:
      - "8080:8080"

volumes:
  bwdata:

